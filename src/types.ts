export type SelectOption = {
  text: string;
  value: string;
};

export type Launch = {
    details: string,
    wikipedia: string,
    launchDate: string,
    patchImg: string,
    name: string,
    image: string,
    youtubeId: string,
    id: string,
    rocketId: string
};

export type Option = {
  date_utc: {},
  success?: {}
};

export type Rocket = {
  id: string,
  name: string
};
