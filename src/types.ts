export type SelectOption = {
  text: string;
  value: string;
};

export type Launch = {
    details: string,
    wikipedia: string,
    launchDate: string,
    patchImg: string,
    name: string,
    rocket: string,
    image: string,
    youtubeId: string,
};

export type Option = {
  date_utc: {},
  success?: boolean
}